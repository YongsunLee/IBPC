#pragma once

class Octree {
public:
	Octree();
	/*
	~Octree();

	void AddChildNode(Octree* child);
	bool AddObject(Vertex* vertex);

	void CullNode(bool culled);

	const Octree* GetChild();

	void SetParent(const Octree* p);
	bool IsInNode(const glm::vec3 pos);

	void Render();
	bool IsCulled();
	*/

private:
	/*
	BoundingBox m_boundingbox;
	std::vector<Octree*> m_vChildren;
	std::list<Vertex*> m_objects;
	bool m_bCulled;
	Octree* m_pParent;
	*/


};